The find_kmers_with_mismatches_and_reverse_complements script finds the most frequent k-mers counting reverse complements and 
sub-sequences within hamming-distance d in a sequence. It can be run from the command line by specifying option -s, -k, and -d. This can be useful for finding potential DnaA boxes in an ori.
